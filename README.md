# Hadoop cluster with Docker

## How to run

Setting up a hadoop cluster with Docker

```bash
# Build image
make hadoop

# Run containers
docker compose up -d

# Stop containers
docker compose stop
```