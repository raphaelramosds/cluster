{
  "name": "mongodb", 
  "config": {
    "connector.class": "io.debezium.connector.mongodb.MongoDbConnector",
    "tasks.max": "1",
    "mongodb.connection.string":"mongodb://fred:root@mongodb:27017/?replicaSet=rs0",
    "topic.prefix": "meu-topico"
  }
}